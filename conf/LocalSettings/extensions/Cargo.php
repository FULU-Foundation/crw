<?php

# Load the Cargo extension, which allows structured storage and retrieval of tabular data.
# Cargo is an alternative to Semantic MediaWiki that stores data in separate SQL tables
# for better performance and scalability.
wfLoadExtension( 'Cargo' );
